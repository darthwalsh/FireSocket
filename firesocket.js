"use strict";

require("firebase");
// require("firebase/auth");
require("firebase/database");

class FireSocket {
  constructor(address) {
    this.address = address;
  }
}

module.exports = FireSocket;
